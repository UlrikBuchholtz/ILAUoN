# Initial upstream fixture coverage

This is a small hand-authored fixture, not a converter or a completed migration review. Exact legacy sources were inspected.

| Source | Fixture coverage and IDs |
| --- | --- |
| `src/matrix-mult.xml:350-381` | Retains `matrix-mult-eg-mult1`; manually expands nested products to `pmatrix`. Product dimensions use self-contained `textcolor` commands with a color-independent explanation. |
| `src/matrix-mult.xml:704-774` | `pilot-reflection-steps`: reflection across `xy`, projection onto `yz`, all three basis-vector images for both maps and their composite, and matrices `B`, `A`, `AB`. Three independent `image/latex-image` elements (`pilot-reflection-e1/e2/e3`) reuse `\pilotreflection` from `docinfo/latex-image-preamble`. Editorial redrawings replace the legacy `drawarrow` helper and custom TikZ styles with standard TikZ, scoped drawing options, labeled planes/axes, and paired before/after arrows. Not a pixel-identical reproduction; no fourth interactive is added. |
| `src/determinant-definitions-properties.xml:1036-1124` | `pilot-multilinearity` is titled `paragraphs`, retaining proposition `det-defn-linear-prop` and proof `pilot-multilinearity-proof`. Explicit editorial shortened proof substitutes cofactor expansion for the original row-operation case analysis, proving both additivity and scaling for any row, including `n=1`. It relies on cofactor expansion rather than reproducing the legacy development order. |
| `src/determinant-definitions-properties.xml:768-781` | `pilot-product-invertible` and `pilot-product-invertible-proof`: full determinant argument for a product of square matrices being invertible iff every factor is invertible. Legacy external cross-references become self-contained statements of multiplicativity and the determinant criterion. This is the visible-proof exception. |
| `src/linindep.xml:317-322` | Retains `linindep-not-any` as a standard `warning`; preserves the distinction between any and at least one redundant vector, includes the original three-vector counterexample, and adds its dependence relation and zero-second-coordinate explanation. External cross-reference omitted. |
| `src/row-reduction.xml:217-309` | Retains `systems-eqns-example1b`; includes the system, every augmented-matrix operation, back-substitution, and the solution-set invariant. External excerpt cross-references are replaced by self-contained text. |
| `src/dimension.xml:33-64` | Retains `dimension-defn-basis`; provisional `essential` to `definition` titled **Essential: Basis**, ordinary ordered-basis `remark`, and `bluebox` to titled `insight`. |
| `src/vectors.xml:55-68` | Provisional `specialcase` to visible `example`, `latex-code` to `image/latex-image`, with standard TikZ preamble and text description. The original special case has no ID; new IDs start with `pilot-`. |
| `src/fundamental-subspaces.xml:367-417` | Preserves all six columns, all four data rows, header, and row/column distinction. Adds acronym context and an explicitly synthetic footnote. Explicit line breaks in the long header/basis cells and column widths `23/10/14/12/23/18%` reduce layout pressure without dropping text. No original table ID exists. |
| `src/lu-decomposition.xml:1318-1338` | Static `program` inside an excerpt retaining enclosing legacy `mpp-eg1` (`specialcase`, lines 1290-1342). Original float literals and operations retained; original output verified by native Python 3.12/SymPy 1.14.0 execution. Browser execution is not yet tested. This source is inactive in the baseline. |

## Embedding and remaining gaps

- Initial read-only syntax reference: installed `/home/ulrik/.ptx/2.52.2/core/schema/pretext-dev.rnc:7-43` supports `interactive iframe`, `width`, `aspect`, and `shortdescription`. These remain experimental constructs in pinned CLI 2.52.3; explicit development-schema validation now passes.
- The inspected 2.52.2 `pretext-html.xsl:10191-10206` prefixes local iframe paths with the external directory. `docinfo/directories external="external"` and `latex-image-preamble` follow `pretext.rnc:2148-2174`. Baseline demo assets and their dependencies belong in `source/external/demos/`; staging is handled by project tooling, not this source fixture.
- Exactly three local embeds: `demos/vector.html` and `demos/rowred1.html` without queries, plus `demos/compose3d.html` with the original matrices, all three ranges off, range 5, and closed true. Composition parameters match `src/matrix-mult.xml:190`. No legacy ID is borrowed for the newly assembled demo paragraphs.
- Adjacent prose/math provides static alternatives for each demo, including PDFs, independently of screenshot generation. There are no authored preview images or `static/image` substitutes yet. Automatic screenshots, HTML, print PDF, and browser checks were exercised in run-005; see `initial-results.md` for failures and limitations.
- The row-reduction static alternative works the exact system opened by `rowred1.html`. The vector alternative describes the actual default `(5,3,4)` as a three-dimensional displacement. The composition alternative gives the two maps and their composite in three separate displays. The original nested matrix product remains intact as an accessible local-scroll hard case, not flattened to avoid overflow.
- Custom `essential`, `bluebox`, `specialcase`, `mathbox`, `latex-code`, and `hide-type` semantics/styling are not reproduced. The provisional standard-element mappings preserve content, not legacy visual identity, numbering, or disclosure behavior.
- Disclosure contract for the separate HTML/publication agent: globally visible insights and warnings; `pilot-multilinearity` paragraphs initially hidden, its `pilot-multilinearity-proof` proof default hidden, and `pilot-product-invertible-proof` explicitly visible. Fixed IDs are the integration hooks, not source visibility attributes. No unsupported `visible` or `hide-type` attributes are authored. Remarks retain the existing hidden-content test. Historical run-005 found insights hidden by the remark setting; the expanded source alone does not resolve or verify that rendering behavior.
- TikZ generation, MathJax/LaTeX color support, nested matrix layout, six-column table width on mobile/PDF, and footnote placement require rendered-output checks. No custom macro package is imported.
- Copyright/license provenance is recorded in the fixture comment; a standalone distribution would also need the applicable license material, not merely the repository-relative reference.

## Checks performed

- Python standard-library `xml.etree.ElementTree` parsed `source/main.ptx`; assertions passed for unique IDs, resolved internal cross-references, exactly three interactives, and six cells in each of the five table rows (header plus four data rows).
- The first schema attempt, using lxml with installed 2.52.2 `pretext-dev.rng`, was failed tooling: schema construction stopped before document validation with `RelaxNGParseError: Internal found no define for ref Group, line 4599`. No installed schema was patched.
- A subsequent actual Jing run in the fresh pinned CLI 2.52.3 environment reported six deprecated `me` elements, the basis `ol` outside a paragraph, and a table with both title and caption. All six displays now use `md number="no"` without `mrow`; the basis list is inside its introductory paragraph; the table retains its title and moves the caption description unchanged to adjacent prose. Run-005 validates without development-schema errors or validation-plus messages; it identifies the three experimental interactives.
- Native Python 3.12/SymPy 1.14.0 execution confirmed the original recorded output. Browser execution has not yet been tested; the fixture program remains static.
- HTML and conventional PDF builds, automatic previews, Chromium checks, and PDF captures are recorded in `initial-results.md`. PDF-FO and the overall Phase 1 gate remain open.
- Expanded-fixture source syntax was checked against the pinned 2.52.3 schema definitions for `paragraphs`, table column widths, and `cell/line`; all displays use `md number="no"` and the ordered list remains inside `p`. No known schema syntax doubt remains from this inspection, but this expansion has not yet been Jing-validated or TikZ-built here. Parent tooling owns full build, disclosure, and rendered mobile/PDF overflow verification; earlier run-005 validation does not establish validation of these additions.
- Source tests now cover the fixed disclosure hooks and absence of unsupported attributes, separated composition displays, shared TikZ helper calls and their reflection coordinates, all basis-vector formula chains and matrix product, multilinearity identities in every row for sizes 1 through 3, the invertible-product proof, and the warning counterexample. Existing nested-matrix and row-operation correctness checks remain; table comparisons normalize whitespace only and retain exact mathematical text.
- Expanded-fixture test run: `/home/ulrik/tmp/ila/phase1/venv/bin/python -m unittest discover -s migration/phase1/tests -p test_fixture.py -v` passed all 12 tests (previously 7; five added). Existing ID and table tests were extended, with no correctness assertions removed.
